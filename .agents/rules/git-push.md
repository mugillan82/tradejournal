# Git Push Workflow Rule

Always automatically commit and push any approved or implemented code changes to git (`origin/main`) after verifying with `npm run build`. This ensures that Vercel continuous deployment automatically receives and deploys the latest updates to production immediately.
