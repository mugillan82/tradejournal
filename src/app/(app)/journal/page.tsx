/**
 * Daily Journal Page — Route Component
 *
 * Route: `/journal` and `/daily-journal`
 */

import { DailyJournalView } from "@/components/journal/daily-journal-view";

export default function JournalPage() {
  return <DailyJournalView />;
}
